# Configure these variables in your IDE/shell. Spring Boot does not auto-load .env.
SPRING_PROFILES_ACTIVE=dev
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=pcforge
DB_USER=pcforge
DB_PASSWORD=
SERVER_PORT=8080
CORS_ALLOWED_ORIGINS=http://localhost:5173
# Required for prod; use the deployment host and TLS parameters as appropriate.
# DB_URL=jdbc:mysql://mysql-host:3306/pcforge?connectionTimeZone=UTC&forceConnectionTimeZoneToSession=true

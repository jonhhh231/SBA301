spring:
  datasource:
    url: ${DB_URL}

server:
  error:
    include-message: never
    include-stacktrace: never
    include-binding-errors: never

# Contract for future CORS configuration; supply the deployed frontend origin.
app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS}
